# Agent、大模型的上下文、Skill 三者关系

## 关系概述
这三个概念共同构成了 AI 智能体（Agent）工作的核心机制：

- **上下文（Context）**：是 Agent 的“工作记忆”，包含当前任务相关的所有信息（如对话历史、用户指令、外部数据）。
- **Agent**：是执行任务的“大脑”，它接收上下文信息，调用可用的 Skill，生成最终的输出或行动。
- **Skill**：是 Agent 的“专业技能包”，将特定领域的知识和操作流程封装成可复用的模块。

## 交互流程
```mermaid
graph TD
    A[用户输入] --> B[上下文 Context]
    B --> C[Agent 智能体]
    C --> D{需要调用 Skill?}
    D -->|是| E[加载 Skill]
    E --> F[执行 Skill 逻辑]
    F --> G[生成结果]
    D -->|否| G
    G --> H[返回给用户]

## 个人理解

在我看来，Agent、上下文和 Skill 之间的关系，就像“一个会动脑子的外卖员”：

- **上下文**是外卖员手里的订单小票——上面写着客户要什么、地址在哪、备注“不要辣”。没有小票，外卖员再聪明也不知道往哪跑。
- **Agent**就是这个外卖员本人——他负责看小票、规划路线、判断先送哪单，根据当前情况做出决策。
- **Skill**是他的专业技能包——比如用地图导航、拍照留证、切换骑行模式。这些技能提前准备好，遇到对应场景直接调用，不用每次重新学一遍。

**三者如何配合**：当上下文告诉 Agent“下雨了，客户要求放门口”，Agent 就会调用“防雨包装”和“拍照留证”两个 Skill，而不是调用“保温箱”这个 Skill。所以，上下文决定“感知什么”，Agent 决定“做什么”，Skill 决定“怎么做”。

通过这次作业，我才真正理解了为什么 WorkBuddy 要设计 Skill 这个机制——好的 AI 助手不是靠一个万能模型回答所有问题，而是靠“读懂当前上下文 + 调对合适的技能”来解决问题。这也让我意识到，以后写 Skill 时要尽量把流程拆得清晰、说明写得完整，这样 Agent 才能准确调用。